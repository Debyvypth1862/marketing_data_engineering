{{ config(
    materialized = "table",
    database = env_var('RAW_DATABASE'),
    schema = "SAPPHIREBET",
    tags = [ "top-level" ],
) }}

SELECT
    CAST(SITEID AS INTEGER) AS SITEID,
    SITENAME,
    CAST(MEDIAID AS INTEGER) AS MEDIAID,
    SUBID,
    CAST(PLAYERID AS INTEGER) AS PLAYERID,
    CAST(REGISTRATIONDATE AS TIMESTAMP) AS REGISTRATIONDATE,
    COUNTRY,
    CAST(DEPOSITAMOUNT AS FLOAT) AS DEPOSITAMOUNT,
    CAST(BETSAMOUNT AS FLOAT) AS BETSAMOUNT,
    CAST(BONUSAMOUNT AS FLOAT) AS BONUSAMOUNT,
    CAST(COMPANYPROFIT AS FLOAT) AS COMPANYPROFIT,
    CAST(COMISSIONRS AS FLOAT) AS COMISSIONRS,
    CAST(CPA AS INTEGER) AS CPA,
    CAST(COMISSIONAMOUNT AS FLOAT) AS COMISSIONAMOUNT,
    TRY_CAST(HOLDTIME AS TIMESTAMP) AS HOLDTIME,
    BLOCKED,
    CLICKID,
    __TYPENAME AS TYPE_NAME,
    TRACKER_LOGIN_ID,
    CURRENT_TIMESTAMP() AS UPDATE_TIME
FROM {{ source('SAPPHIREBET', '_AIRBYTE_RAW_PLAYERS_REPORT') }}
