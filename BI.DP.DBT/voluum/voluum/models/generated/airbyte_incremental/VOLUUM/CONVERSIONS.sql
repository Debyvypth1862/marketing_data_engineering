{{ config(
    cluster_by = ["_AIRBYTE_UNIQUE_KEY", "_AIRBYTE_EMITTED_AT"],
    unique_key = "_AIRBYTE_UNIQUE_KEY",
    database = env_var('RAW_DATABASE'),
    schema = "VOLUUM",
    tags = [ "top-level" ]
) }}
-- Final base SQL model
-- depends_on: {{ ref('CONVERSIONS_SCD') }}
select
    _AIRBYTE_UNIQUE_KEY,
    ALL_CONVERSIONS_REVENUE,
    AFFILIATE_NETWORK_ID,
    AFFILIATE_NETWORK_NAME,
    BRAND,
    BROWSER,
    BROWSER_VERSION,
    CAMPAIGN_ID,
    CAMPAIGN_NAME,
    CAMPAIGN_URL_CONFIGURED,
    CITY,
    CLICKID,
    CONNECTION_TYPE,
    CONNECTION_TYPE_NAME,
    CONVERSION_ORIGINAL_CURRENCY,
    CONVERSION_TYPE,
    CONVERSION_TYPE_ID,
    COST,
    COST_SOURCES,
    COUNTRY_CODE,
    COUNTRY_NAME,
    CUSTOM_VARIABLE_1,
    CUSTOM_VARIABLE_10,
    CUSTOM_VARIABLE_2,
    CUSTOM_VARIABLE_3,
    CUSTOM_VARIABLE_4,
    CUSTOM_VARIABLE_5,
    CUSTOM_VARIABLE_6,
    CUSTOM_VARIABLE_7,
    CUSTOM_VARIABLE_8,
    CUSTOM_VARIABLE_9,
    DEVICE,
    DEVICE_NAME,
    EXTERNAL_ID,
    FLOW_ID,
    IP,
    IS_DSP,
    ISP,
    LANDER_ID,
    LANDER_NAME,
    MOBILE_CARRIER,
    MODEL,
    OFFER_ID,
    OFFER_NAME,
    OS,
    OS_VERSION,
    OUTGOING_POSTBACK_URL,
    PATH_ID,
    POSTBACK_PARAM_1,
    POSTBACK_PARAM_2,
    POSTBACK_PARAM_3,
    POSTBACK_PARAM_4,
    POSTBACK_PARAM_5,
    POSTBACK_TIMESTAMP,
    REFERRER,
    REGION,
    REVENUE,
    REVENUE_IN_ORIGINAL_CURRENCY,
    SUB_LANDER_ID,
    TRAFFIC_SOURCE_ID,
    TRAFFIC_SOURCE_NAME,
    TRANSACTION_ID,
    TYPE,
    USER_AGENT,
    VISIT_TIMESTAMP ,
    _AIRBYTE_AB_ID,
    _AIRBYTE_EMITTED_AT,
    S3_PATH,
    {{ current_timestamp() }} as _AIRBYTE_NORMALIZED_AT,
    _AIRBYTE_CONVERSIONS_HASHID
from {{ ref('CONVERSIONS_SCD') }}
-- CONVERSIONS from {{ source('VOLUUM', '_AIRBYTE_RAW_CONVERSIONS') }}
where 1 = 1
and _AIRBYTE_ACTIVE_ROW = 1
{{ incremental_clause('_AIRBYTE_EMITTED_AT', this) }}

