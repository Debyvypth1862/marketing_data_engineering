{{ config(
    cluster_by = ["_AIRBYTE_UNIQUE_KEY", "_AIRBYTE_EMITTED_AT"],
    unique_key = "_AIRBYTE_UNIQUE_KEY",
    database = env_var('RAW_DATABASE'),
    schema = "REFERON",
    tags = [ "top-level" ]
) }}
-- Final base SQL model
-- depends_on: {{ ref('DYNAMIC_VARIABLES_REPORT_SCD') }}
select
    _AIRBYTE_UNIQUE_KEY,
    CLICK_COUNT,
    TRAFFIC_TYPE,
    FTDS_DEPOSITS,
    BANNER_VIEW_COUNT,
    MARKETING_SOURCE,
    DYNAMIC_VARIABLES,
    AVG_DEPOSIT,
    PROGRAM_ID,
    TURNOVER,
    CPA_COUNT,
    UNIQUE_CLICK_COUNT,
    DEPOSITS_PER_FTD,
    COUNT_OF_DEPOSITS,
    BRAND_ID,
    ACTIVE_CUSTOMERS,
    DEPOSITS,
    REG_TO_FTD,
    MARKETING_SOURCE_ID,
    MEDIA_ITEM_ID,
    CPC_COUNT,
    GEO,
    CUSTOMER_ID,
    CLICK_ID,
    MEDIA_ITEM_NAME,
    NGR,
    PUBID,
    PRODUCT_ID,
    PROGRAM_NAME,
    MEDIA_CAMPAIGN_NAME,
    FTD_COUNT,
    REWARDING_PLAN,
    CUSTOMER_REG_DATE,
    NDC,
    SUBID,
    VAR5,
    VAR4,
    CLICK_TO_FTD,
    RS_DISTRIBUTION,
    VAR3,
    VAR2,
    VAR1,
    DEPOSITING_CUSTOMERS,
    PRODUCT_NAME,
    MEDIA_CAMPAIGN_ID,
    TOTAL_REWARD,
    CLICKID,
    REVENUE_SHARE,
    DATE,
    BRAND_NAME,
    REG_COUNT,
    CPA,
    CPC,
    FIXED_FEE,
    TRACKER_LOGIN_ID,
    _AIRBYTE_AB_ID,
    _AIRBYTE_EMITTED_AT,
    S3_PATH,
    {{ current_timestamp() }} as _AIRBYTE_NORMALIZED_AT,
    _AIRBYTE_DYNAMIC_VARIABLES_REPORT_HASHID
from {{ ref('DYNAMIC_VARIABLES_REPORT_SCD') }}
-- DYNAMIC_VARIABLES_REPORT from {{ source('REFERON', '_AIRBYTE_RAW_DYNAMIC_VARIABLES_REPORT_STREAM') }}
where 1 = 1
and _AIRBYTE_ACTIVE_ROW = 1
{{ incremental_clause('_AIRBYTE_EMITTED_AT', this) }}

