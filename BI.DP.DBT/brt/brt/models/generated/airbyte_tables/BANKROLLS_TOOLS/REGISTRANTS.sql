{{ config(
    cluster_by = ["_AIRBYTE_EMITTED_AT"],
    unique_key = '_AIRBYTE_AB_ID',
    schema = "BRT",
    database = env_var('RAW_DATABASE'),
    tags = [ "top-level" ]
) }}
-- Final base SQL model
-- depends_on: {{ ref('REGISTRANTS_STG') }}
select
    
    COUNTRY,
    KEYWORDS,
    FAVORITE_GAME,
    FORM_UUID,
    LANGUAGE,
    ISP_TYPE,
    CASINO_OFFER_PREFERENCE,
    UA_JSON,
    SCORE,
    AFFILIATE_PAYMENT_STATUS,
    PASSWORD,
    DISPOSABLE_EMAIL,
    ID,
    STATE,
    IS_MALE,
    BROWSER_VERSION,
    MAXMIND_DATA,
    AFFILIATE_PAYMENT_DATE,
    CPU,
    STATIC_IP_SCORE,
    VERIFIED_EMAIL,
    SCORE_FRAUD,
    VERIFICATION_EMAIL_SENT,
    PHONE,
    USER_ID,
    CLICK_ID,
    FORM_EVENTS_CLICK,
    REGION,
    FORM_EVENTS,
    DEVICE,
    PLAY_FOR,
    CONTINENT,
    OPT_OUT,
    CITY,
    DATE_OF_BIRTH,
    KICKBOX_RESPONSE,
    CREATED_AT,
    UPDATED_AT,
    BROWSER,
    EMAIL_SENDEX_SCORE,
    OPERATING_SYSTEM,
    AGILECRM_ID,
    PUSHED_TO_AGILE_CRM,
    FIRST_NAME,
    EMAIL,
    USER_AGENT,
    SENDABLE_VERIFIED,
    PUSHED_TO_SNOWFLAKE,
    LAST_NAME,
    IP_ADDRESS,
    TIME_ZONE,
    REGISTRATION_DOMAIN,
    URL_PARAMS,
    EMAIL_SENDEX,
    FORM_EVENTS_MOVEMENT,
    SCORE_WHALE,
    PUSHED_TO_FLOWS,
    AFFILIATE_CPL,
    _AIRBYTE_AB_ID,
    _AIRBYTE_EMITTED_AT,
    {{ current_timestamp() }} as _AIRBYTE_NORMALIZED_AT,
    _AIRBYTE_REGISTRANTS_HASHID
from {{ ref('REGISTRANTS_STG') }}
-- REGISTRANTS from {{ source('BRT', '_AIRBYTE_RAW_REGISTRANTS') }}
where 1 = 1


